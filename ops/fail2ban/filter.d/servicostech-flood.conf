# Fail2Ban filter — flood / scraping agressivo: conta TODA requisição logada.
# Combinado com maxretry/findtime no jail, bane IP que excede a taxa.
[Definition]
failregex = access route="[^"]*" path="[^"]*" status=\d+ ms=[0-9.]+ ip="<HOST>"
ignoreregex =
